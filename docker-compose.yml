services:
  mongo: 
    image: mongo:7
    restart: unless-stopped
    volumes: 
      - mongo_data:/data/db
    ports:
      - "27017:27017"
  bot:
    build: .
    restart: unless-stopped
    env_file: 
      - .env
    depends_on:
      - mongo

volumes:
  mongo_data:
